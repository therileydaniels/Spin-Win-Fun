I want to make sure this app is a fully functional PWA (Progressive Web App) so users can install it on their phones. Please audit what exists and complete anything missing.

---

PART 1: AUDIT CURRENT PWA STATUS

Check and report what exists:

1. manifest.json — does it exist in client/public/?
2. Service worker — is there a sw.js file? Is it registered?
3. Meta tags in index.html — are PWA meta tags present?
4. Icons — are icon-192.png and icon-512.png in client/public/icons/?

Report what's present and what's missing.

---

PART 2: COMPLETE MANIFEST.JSON

Ensure client/public/manifest.json exists with:

{
  "name": "QuickWheel",
  "short_name": "QuickWheel",
  "description": "A customizable wheel spinner for decisions and games",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#0f172a",
  "theme_color": "#A855F7",
  "orientation": "portrait",
  "icons": [
    {
      "src": "/icons/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}

---

PART 3: CREATE SERVICE WORKER

Create client/public/sw.js with offline caching:

const CACHE_NAME = 'quickwheel-v1';
const STATIC_ASSETS = [
  '/',
  '/index.html',
  '/manifest.json',
  '/favicon-32x32.png',
  '/icons/icon-192.png',
  '/icons/icon-512.png'
];

// Install - cache static assets
self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => {
      return cache.addAll(STATIC_ASSETS);
    })
  );
  self.skipWaiting();
});

// Activate - clean up old caches
self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((cacheNames) => {
      return Promise.all(
        cacheNames
          .filter((name) => name !== CACHE_NAME)
          .map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

// Fetch - network first, fallback to cache
self.addEventListener('fetch', (event) => {
  // Skip non-GET requests
  if (event.request.method !== 'GET') return;
  
  // Skip API requests - always go to network
  if (event.request.url.includes('/api/')) return;
  
  event.respondWith(
    fetch(event.request)
      .then((response) => {
        // Clone and cache successful responses
        if (response.ok) {
          const responseClone = response.clone();
          caches.open(CACHE_NAME).then((cache) => {
            cache.put(event.request, responseClone);
          });
        }
        return response;
      })
      .catch(() => {
        // Network failed, try cache
        return caches.match(event.request).then((cachedResponse) => {
          if (cachedResponse) {
            return cachedResponse;
          }
          // If it's a navigation request, return the cached index.html
          if (event.request.mode === 'navigate') {
            return caches.match('/index.html');
          }
          return new Response('Offline', { status: 503 });
        });
      })
  );
});

---

PART 4: REGISTER SERVICE WORKER

Add service worker registration to the app. In client/src/main.tsx, add after the app renders:

// Register service worker
if ('serviceWorker' in navigator && import.meta.env.PROD) {
  window.addEventListener('load', () => {
    navigator.serviceWorker.register('/sw.js')
      .then((registration) => {
        console.log('SW registered:', registration.scope);
      })
      .catch((error) => {
        console.log('SW registration failed:', error);
      });
  });
}

Note: Only register in production (import.meta.env.PROD) to avoid caching issues during development.

---

PART 5: VERIFY META TAGS IN INDEX.HTML

Ensure client/index.html has these in the <head>:

<!-- PWA -->
<link rel="manifest" href="/manifest.json">
<meta name="theme-color" content="#A855F7">

<!-- iOS PWA support -->
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="QuickWheel">
<link rel="apple-touch-icon" href="/icons/icon-192.png">

<!-- Viewport for mobile -->
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

---

PART 6: ADD INSTALL PROMPT (OPTIONAL BUT NICE)

Create a component that shows an "Install App" button when the app is installable.

Create client/src/components/InstallPrompt.tsx:

import { useState, useEffect } from 'react';
import { Button } from '@/components/ui/button';
import { Download } from 'lucide-react';

export function InstallPrompt() {
  const [deferredPrompt, setDeferredPrompt] = useState<any>(null);
  const [showInstall, setShowInstall] = useState(false);

  useEffect(() => {
    const handler = (e: Event) => {
      e.preventDefault();
      setDeferredPrompt(e);
      setShowInstall(true);
    };

    window.addEventListener('beforeinstallprompt', handler);

    return () => window.removeEventListener('beforeinstallprompt', handler);
  }, []);

  const handleInstall = async () => {
    if (!deferredPrompt) return;
    
    deferredPrompt.prompt();
    const { outcome } = await deferredPrompt.userChoice;
    
    if (outcome === 'accepted') {
      setShowInstall(false);
    }
    setDeferredPrompt(null);
  };

  if (!showInstall) return null;

  return (
    <Button
      variant="outline"
      size="sm"
      onClick={handleInstall}
      className="gap-2"
    >
      <Download className="h-4 w-4" />
      Install App
    </Button>
  );
}

Then add <InstallPrompt /> to the header component, near the other buttons.

---

PART 7: TEST PWA

After implementing, verify:
1. manifest.json loads (check DevTools > Application > Manifest)
2. Service worker registers (check DevTools > Application > Service Workers)
3. App is installable (look for install icon in browser address bar)
4. Icons appear correctly in manifest

Report what was added/changed and confirm PWA is working.