I need to improve the UX around the settings panel to keep the probability rigging more hidden. The wheel's rigging feature should be subtle — not obvious to viewers.

---

CHANGE 1: SETTINGS PANEL CLOSED BY DEFAULT

The Wheel Settings panel should be COLLAPSED/CLOSED by default when:
- User is not signed in
- User first loads the page
- User just signed in

The user should have to click to open it. This keeps the rigging hidden from casual viewers.

---

CHANGE 2: AUTO-CLOSE SETTINGS AFTER SAVE

When a user saves their wheel (clicks Save and the save is successful):
- Automatically close/collapse the settings panel
- Show a brief success toast "Wheel saved!"
- The wheel becomes the main focus

---

CHANGE 3: WHEEL FOCUS MODE WHEN SETTINGS CLOSED

When the settings panel is closed/collapsed:
- The wheel should be more centered on the page
- Wheel can be slightly larger (more prominent)
- Just show the wheel + spin button as the main focus
- Add a small subtle button or icon to re-open settings (gear icon or "Settings" text button)

When settings panel is open:
- Current layout is fine (wheel on left, settings on right)

---

CHANGE 4: SETTINGS TOGGLE BUTTON

Add a clear but subtle way to toggle the settings panel:
- Small gear icon or "⚙ Settings" button near the wheel
- Or a collapsible panel with an arrow/chevron to expand/collapse
- Should be obvious enough for the operator to find, but not attention-grabbing for viewers

---

BEHAVIOR SUMMARY:

| State | Settings Panel | Wheel Size |
|-------|---------------|------------|
| First visit (not signed in) | Closed | Centered, prominent |
| Just signed in | Closed | Centered, prominent |
| User clicks to open settings | Open | Side-by-side layout |
| User saves wheel | Auto-closes | Centered, prominent |
| Presentation mode | Hidden completely | Full focus |

---

The goal: Someone watching a stream or presentation should just see a beautiful wheel, not a settings panel that reveals probability controls.

Please implement these changes.