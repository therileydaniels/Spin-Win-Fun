Please fix the following issues found in our security audit:

---

CRITICAL FIX 1: SERVER-SIDE SEGMENT VALIDATION

In the wheel API endpoints (POST /api/wheels and PUT /api/wheels/:id), add validation for:

1. Segment count: Must be between 2 and 20 segments
2. Segment label length: Each label must be max 25 characters
3. Segment color format: Must be a valid hex color (e.g., #A855F7)

Use the existing constants from schema.ts: MIN_SEGMENTS = 2, MAX_SEGMENTS = 20, MAX_LABEL_LENGTH = 25

Return a 400 error with a clear message if validation fails:
- "Wheel must have between 2 and 20 segments"
- "Segment label cannot exceed 25 characters"
- "Invalid segment color format"

---

MEDIUM FIX 2: REMOVE EMAIL FROM CONSOLE LOG

In server/routes.ts around line 87, there's a console.log that includes the user's email on duplicate signup attempts. Remove the email from the log or remove the log entirely. Just log "Signup attempt for existing email" without the actual email address.

---

MEDIUM FIX 3: WHEEL NAME MAX LENGTH

In the wheel API endpoints, add validation:
- Wheel name must be max 100 characters
- Return 400 error: "Wheel name cannot exceed 100 characters"

---

MEDIUM FIX 4: SESSION SECRET HANDLING

In server/index.ts, the session secret has a hardcoded fallback. Change this so it throws an error if SESSION_SECRET environment variable is not set in production. Only allow the fallback in development mode.

---

MINOR FIX 5: TYPE ERROR HANDLING

In catch blocks that use "any" type, change to "unknown" and add proper type guards. Check if error is an instance of Error before accessing error.message. Otherwise show a generic error message.

Apply this to:
- client/src/components/AuthModal.tsx (around lines 84 and 113)
- client/src/pages/Home.tsx (around line 83)
- server/index.ts (around line 100)

---

MINOR FIX 6: MOBILE UX IMPROVEMENT

In the ProbabilityPanel, make the segment input row more touch-friendly on mobile:
- Increase probability input width slightly (w-16 instead of w-14)
- Add a bit more vertical spacing between rows on mobile if needed

---

Please make these fixes and confirm each one is complete.