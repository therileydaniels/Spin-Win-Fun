node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.*
!.env.example
.mcp.json
.playwright-mcp/

# OS / editor junk
Thumbs.db

# impeccable live-mode runtime journal (keep config + design.json, ignore session state)
.impeccable/live/sessions/
# Brainstorming visual companion (local mockups)
.superpowers/
